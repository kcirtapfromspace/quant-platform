[package]
name = "quant-risk"
version = "0.1.0"
edition = "2021"
description = "Pure-Rust risk engine: position sizing, exposure limits, circuit breaker"

[lib]
crate-type = ["rlib"]

[dependencies]
thiserror = "1"

[dev-dependencies]
approx = "0.5"
