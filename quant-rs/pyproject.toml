[build-system]
requires = ["maturin>=1.5,<2.0"]
build-backend = "maturin"

[project]
name = "quant_rs"
version = "0.1.0"
requires-python = ">=3.10"
description = "PyO3 Python bindings for quant-rs Rust compute kernels"

[tool.maturin]
features = ["extension-module"]
