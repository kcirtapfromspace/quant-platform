apiVersion: v1
kind: Service
metadata:
  name: quant-dashboard
  namespace: hypothesis-validation
spec:
  selector:
    app: quant-dashboard
  ports:
    - name: http
      port: 80
      targetPort: 3001
  type: ClusterIP
